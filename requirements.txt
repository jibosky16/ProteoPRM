# PRM Quant Software Dependencies  (pinned from .venv311, 2025-06-09)
# Install with: pip install -r requirements.txt
# Regenerate with: .venv311\Scripts\pip freeze

# ── Core Scientific Computing ───────────────────────────────────────────────
numpy==1.26.4
scipy==1.17.0
pandas==2.3.3
scikit-learn==1.8.0

# ── Data Format Support ─────────────────────────────────────────────────────
pyteomics==4.7.5
openpyxl==3.1.5
pillow==12.1.1
pyarrow==23.0.1

# ── UI and Visualization ────────────────────────────────────────────────────
matplotlib==3.10.8
seaborn  # pulled in by peptdeep; no separate pin in freeze
sv-ttk==2.6.1

# ── Utilities ────────────────────────────────────────────────────────────────
tqdm==4.67.3
requests==2.32.5
psutil==7.2.2
lxml==6.0.2

# ── Build Tools ──────────────────────────────────────────────────────────────
pyinstaller>=6.0

# ── MS data / Rescoring / Prediction ────────────────────────────────────────
mokapot==0.10.0
psm-utils==1.5.2
ms2pip==4.1.2
peptdeep==1.4.1
fisher-py==2.0.2

# ── DeepLC (deep-learning retention time prediction) ────────────────────────
deeplc==3.1.13
deeplcretrainer==1.0.2
tensorflow==2.21.0
keras==3.13.2

# ── Torch (required by peptdeep / alphabase) ────────────────────────────────
torch==2.10.0

# ── Additional transitive deps (pinned for reproducibility) ─────────────────
absl-py==2.4.0
alphabase==1.8.1
alpharaw==0.5.0
alphatims==1.0.9
biopython==1.86
certifi==2026.1.4
click==8.3.1
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
et_xmlfile==2.0.0
filelock==3.24.3
flatbuffers==25.12.19
fonttools==4.61.1
gast==0.7.0
google-pasta==0.2.0
grpcio==1.78.0
h5py==3.14.0
huggingface_hub==1.4.1
idna==3.11
jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.4.9
libclang==18.1.1
llvmlite==0.46.0
markupsafe==3.0.3
ml_dtypes==0.5.4
mpmath==1.3.0
namex==0.1.0
networkx==3.6.1
numba==0.64.0
opt_einsum==3.4.0
optree==0.19.0
packaging==26.0
protobuf==7.34.0
psims==1.3.5
pyahocorasick==2.3.0
pydantic==2.12.5
pydantic_core==2.41.5
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pythonnet==3.0.5
pytz==2025.2
pyyaml==6.0.3
rdkit==2025.9.5
safetensors==0.7.0
six==1.17.0
SQLAlchemy==2.0.46
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.22.2
transformers==5.2.0
triqler==0.7.3
typing_extensions==4.15.0
urllib3==2.6.3
wrapt==2.1.2
xgboost==2.1.4
zipp==3.23.0
xlrd==2.0.1
xlsxwriter==3.1.4
# ============================================================================
# OPTIONAL: GPU ACCELERATION
# ============================================================================
# For NVIDIA GPU acceleration, install the appropriate packages for your CUDA version.
# These are NOT required - the software will run on CPU if not installed.
#
# Option 1: CuPy (GPU-accelerated NumPy) - Recommended for basic GPU acceleration
# Check your CUDA version with: nvcc --version or nvidia-smi
# Then install the matching CuPy version:
#   CUDA 11.x: pip install cupy-cuda11x
#   CUDA 12.x: pip install cupy-cuda12x
#
# Option 2: RAPIDS cuML (GPU-accelerated Machine Learning) - For advanced ML acceleration
#
# IMPORTANT: cuML is primarily designed for conda installation and has limited pip support.
#
# Method A - Conda (Recommended if you have conda):
#   conda install -c rapidsai -c conda-forge -c nvidia cuml
#
# Method B - pip (Limited support, may not work with all Python versions):
#   pip install --extra-index-url https://pypi.nvidia.com cuml-cu12  # For CUDA 12
#   pip install --extra-index-url https://pypi.nvidia.com cuml-cu11  # For CUDA 11
#
# Method C - Skip cuML entirely:
#   The software uses scikit-learn as a fallback, which works great on CPU.
#   cuML provides ~10-50x speedup on GPU for ML operations, but most of the
#   compute time in this software is spent on I/O and fragment matching,
#   not ML operations. So skipping cuML has minimal impact.
#
# Platform Notes:
#   - cuML works best on Linux
#   - Windows requires WSL2 for full RAPIDS support
#   - If pip install fails, don't worry - scikit-learn will be used automatically
#
# Option 3: PyTorch with CUDA (alternative GPU detection)
# pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# ============================================================================
# GPU REQUIREMENTS:
# - NVIDIA GPU (GTX 1000 series or newer recommended)
# - NVIDIA drivers installed (verify with: nvidia-smi)
# - CUDA Toolkit (for building from source, not needed for pip packages)
#
# If you have Intel or AMD GPU:
# - These GPUs are not supported for CUDA acceleration
# - The software will automatically use CPU processing (which is still fast!)
# ============================================================================
